import {_bg_color, container} from "../../../Theme/styles";

const styles = {
  reviews: {
    backgroundColor: _bg_color,
    paddingTop: 100,
    paddingBottom: 130,
    textAlign: 'center',
  },
  container,
};

export { styles };
